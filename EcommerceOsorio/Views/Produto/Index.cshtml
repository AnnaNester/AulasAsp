@using EcommerceOsorio.Models

@model List<Produto>
@{
    DateTime data = ViewBag.Data;
}

<div>
        <h2>Gerenciamento de Produtos</h2>
        @*<a href="~/Views/Cadastrar">Cadastrar Produto</a>*@


        @* Link em MVC | Razor *@
    <div>
        @Html.ActionLink("Cadastrar Produto", "CadastrarProduto", "Produto", new { @class = "li nav-item" })
    </div>
        <br />
        <br />
            <table class="table table-hover table-striped" style="margin-top:30px">
                <tr>
                    <th>Nome</th>
                    <th>Descrição</th>
                    <th>Preço</th>
                    <th>Categoria</th>
                    <th>Imagem</th>
                    <th>Alterar</th>
                    <th>Remover</th>
                </tr>
                @foreach (var produto in Model)
            {
                    <tr>
                        <td>@produto.NomeProduto</td>
                        <td>@produto.DescricaoProduto</td>
                        <td>@produto.PrecoProduto</td>
                        <td>@produto.CategoriaProduto.NomeCategoria</td>
                        <td><img src="~/Images/@produto.ImagemProduto" alt="Imagem do Produto" style="width:100px; height:100px"/></td>
                        <td>@Html.ActionLink("Alterar", "AlterarProduto", "Produto", new { id = produto.ProdutoId }, new { @class = "btn btn-info" })</td>
                        <td>@Html.ActionLink("Remover", "RemoverProduto", "Produto", new { id = produto.ProdutoId }, new { @class = "btn btn-danger" })</td>
                    </tr>
                }
            </table>

            <p>Dados atualizados às: <b> @data</b></p>
        </div>
